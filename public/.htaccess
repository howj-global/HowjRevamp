# Hostinger (Apache) config for the built SPA.
# Vite copies everything in public/ into dist/, so this ships with the build —
# upload the CONTENTS of dist/ into public_html/ and this lands beside index.html.

# ---------------------------------------------------------------------------
# SPA fallback — REQUIRED.
# React Router owns /about, /expression/brazil, etc. Those paths don't exist as
# files on disk, so without this Apache 404s on any deep link or page refresh.
# ---------------------------------------------------------------------------
# Don't 301 /about -> /about/. public/ ships asset folders whose names collide
# with real routes (about, expressions, gallery), and that redirect fires before
# the rewrite below, landing on a directory Apache then refuses to list (403).
DirectorySlash Off

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Only real FILES bypass the app (assets, images, favicon…).
  # Deliberately NOT matching directories: /about, /expressions and /gallery all
  # exist as asset folders AND as React routes, and the route has to win. No
  # directory here holds an index.html, so nothing is shadowed by this.
  RewriteCond %{REQUEST_FILENAME} -f
  RewriteRule ^ - [L]

  # Everything else hands off to the app
  RewriteRule . /index.html [L]
</IfModule>

# ---------------------------------------------------------------------------
# Compression
# ---------------------------------------------------------------------------
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/css text/javascript
  AddOutputFilterByType DEFLATE application/javascript application/json
  AddOutputFilterByType DEFLATE image/svg+xml
</IfModule>

# ---------------------------------------------------------------------------
# Caching. Vite fingerprints asset filenames (index-a1b2c3.js), so they can be
# cached hard — but index.html must always revalidate or users get a stale app
# pointing at deleted asset hashes after a redeploy.
# ---------------------------------------------------------------------------
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType image/jpeg "access plus 6 months"
  ExpiresByType image/png "access plus 6 months"
  ExpiresByType image/webp "access plus 6 months"
  ExpiresByType image/avif "access plus 6 months"
  ExpiresByType image/svg+xml "access plus 6 months"
  ExpiresByType video/mp4 "access plus 6 months"
  ExpiresByType font/woff2 "access plus 1 year"
</IfModule>

<IfModule mod_headers.c>
  <FilesMatch "\.html$">
    Header set Cache-Control "no-cache, must-revalidate"
  </FilesMatch>
</IfModule>
